.center-content
  .chat
    .chat__name
      = current_user.name
    .chat__date
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
    .chat__message
      = message.body
      = image_tag message.image
