!!!
.html
  .head
    %title
      chat-space
  .body
    .left_content
      = link_to edit_user_registration_path do
        %i.fa.fa-cog
      = link_to new_group_path do
        %i.fa.fa-pencil-square-o.user__icon
      .user
        %p.user__name
          - if user_signed_in?
            = current_user.name

      .left_content_chat
        %ul
          - @groups.each do |group|
            %li
              = group.name



    .right_content
      .top_content
        = link_to 'Edit',"/groups/1/edit", class: "edit_chat"
        %p.name
          グループ名
        %p.member_name
          member :ユーザー
      .center_content
        %ul
          %li
            %p.name
              ユーザー名
            %p.comment
              おはよう!!
        %ul
          %li
            %p.name
              ユーザー名
            %p.comment
              こんにちは!!
      .format
        %input.format__input{class: 'submit', type: 'text',placeholder: "Type a message"}

        %i.fa.fa-file-image-o.format__icon
          = link_to "", "", type: "file", name: "image", class: "icon_tag"

        %button.format__button{ type: "submit"}Send
