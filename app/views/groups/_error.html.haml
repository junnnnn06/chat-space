.chat-group-form__errors
  - if @group.errors.any?
    %h2
      = "#{@group.errors.count}件のエラーが発生しました。"
      %ul
        - @group.errors.full_messages.each do |message|
          %li
            = message
